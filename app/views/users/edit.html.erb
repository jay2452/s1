<h1>Edit user</h1>
<div class="row">
	<div class="span6 offset3">
	<%= form_for(@user) do |f| %>

	<%= render 'fields', f: f %>
      
      <div class="actions">
      	<%= f.submit "Update", class: "btn btn-large btn-primary" %>
      </div>
      
      <% end %>

	</div>
</div>

